So given a url lets take an example https://www.linkedin.com/in/jonshrey we need to shroten it to something like shoretn/jonshrey.
Okay , so basically what our shortener should do is turn normal url to short form and then "redirect" to the original link so basically the shortened link doesnt magically opens the same page it knows that it needs to redirect to hat link. so in short thats how the url shortener will work. now what we need is find a way to encode the original url into shortened form and second we need to find a way so that shortened url is mapped to original . also need link analytics lets talk about it later so we need two end points POST and GET.
Okay so first we need to decide how we will encode the original url we can use something like base62 with idempotency so two url dont point to same shortened string . i can also keep seperate service for POST and GET . one shortening service and one redirecting service. and we can have  a service for link analytics which can just track the number of times the shortened link was clicked. 